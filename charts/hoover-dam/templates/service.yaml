apiVersion: v1
kind: Service
metadata:
  name: {{ include "hoover-dam.fullname" . }}
  labels:
    {{- include "hoover-dam.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hoover-dam.selectorLabels" . | nindent 4 }}
